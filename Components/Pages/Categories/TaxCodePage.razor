@page "/taxcode"

<FluentStack Orientation="Orientation.Vertical">
        <FluentLabel Typo="Typography.H4">@CurrentUser.UserName</FluentLabel>
</FluentStack>

@code {
    private UserInfo CurrentUser = new();
    

}